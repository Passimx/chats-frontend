import styles from './index.module.css';

const LoadingChats = () => {
    return (
        <div id={styles.background}>
            <h1>Search Chats!</h1>
        </div>
    );
};

export default LoadingChats;
