import { FC } from 'react';
import styles from './index.module.css';

const ChatsNotFound: FC = () => {
    return <div id={styles.background}>Чаты не найдены</div>;
};

export default ChatsNotFound;
